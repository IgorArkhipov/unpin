# Unpin v0.1.0-beta.14

`v0.1.0-beta.14` makes large-repository discovery faster and prevents the
terminal UI from appearing stalled while startup work is in progress.

## Highlights

- Independent project-scope scans now run concurrently while retaining stable,
  provider-ordered inventory and diagnostic output.
- `unpin tui` draws immediately, displays provider-by-provider discovery
  progress, and accepts `Q` or `Esc` to cancel before startup completes.
- Discovery and credential resolution run off the terminal event loop, so slow
  configuration walks or Keychain access do not block rendering or input.

## Verification

- This program-logic release requires the full provider matrix and live Pi and
  OpenCode host validation from the exact candidate commit, in addition to
  required CI and locked workspace checks.
- The release workflow verifies the GNU/Linux artifact's `GLIBC_2.35`-or-older
  symbol ceiling and runs `--version` and `--help` in Debian 12.
- Each archive has GitHub build provenance, a CycloneDX SBOM, and a
  workflow-generated `SHA256SUMS` trust root. Verify a downloaded archive with:

```sh
shasum -a 256 -c SHA256SUMS
gh attestation verify unpin-v0.1.0-beta.14-x86_64-unknown-linux-gnu.tar.gz \
  --repo IgorArkhipov/unpin \
  --source-ref refs/tags/v0.1.0-beta.14 \
  --signer-workflow IgorArkhipov/unpin/.github/workflows/release.yml
```

For setup and security guidance, see the
[onboarding guide](https://github.com/IgorArkhipov/unpin/blob/v0.1.0-beta.14/docs/ONBOARDING.md)
and [security policy](https://github.com/IgorArkhipov/unpin/blob/v0.1.0-beta.14/SECURITY.md).
